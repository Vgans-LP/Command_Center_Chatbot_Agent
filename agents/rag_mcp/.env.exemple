# ===== AWS creds/region =====
AWS_PROFILE=default
AWS_REGION=eu-central-1
AWS_DEFAULT_REGION=eu-central-1

# ===== Knowledge Bases (you have exactly these two) =====
# general-docs
KB_GENERAL_DOCS_ID=kb-XXXXXXXXXXXX
# cs-support
KB_CS_SUPPORT_ID=kb-YYYYYYYYYYYY

# ===== MCP KB Server (mcp_kb_server.py) =====
# Retrieval knobs (optional)
TOP_K=8
MIN_SCORE=0.0

# Optional: shared secret that callers (Agent/Gateway) must present as X-Api-Key
# Leave empty to disable auth in dev.
MCP_SERVER_API_KEY=

# ===== Agent (kb_agent.py) =====
# In dev, point to your local MCP server:
MCP_URL=http://127.0.0.1:7000
# In prod, override MCP_URL with your AgentCore Gateway URL, e.g.:
# MCP_URL=https://your-gateway.internal.example/mcp

# If your MCP server requires the X-Api-Key, put it here (must match MCP_SERVER_API_KEY).
MCP_API_KEY=

# Bedrock fallback model (only used if tool-enabled agent call fails)
# Keep this set even in dev so the safety net works.
MODEL_ID=anthropic.claude-sonnet-4-20250514-v1:0

# AgentCore HTTP port for local runs
PORT=8080

# Optional webhook signing when you use callback_url in /invocations payloads
WEBHOOK_SECRET=

# ===== Optional: SSM parameter fallback (usually OFF now that MCP is used) =====
# If you ever want the old KB_ID-from-SSM path, enable and set prefix.
USE_SSM_PARAMS=0
SSM_PARAM_PREFIX=/ACCOUNT-REGION/kb

# ===== A/B harness (ab_harness.py) =====
# Ollama HTTP endpoint
OLLAMA_URL=http://localhost:11434
# Reuse MCP_URL and MCP_API_KEY above for harness → MCP calls
